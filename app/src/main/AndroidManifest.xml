<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.eremovals6">

    <!-- Allows the app to access the internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eremovals6"
        tools:targetApi="31">

        <!-- Main activity of the app; android:exported must be true for activities with intent-filters -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity for adding prompt codes -->
        <activity
            android:name=".AddPromptCodeActivity"
            android:exported="false" />

        <!-- Activity for adding price items -->
        <activity
            android:name=".AddPriceItemActivity"
            android:exported="false" />

        <!-- Activity for displaying promotion codes -->
        <activity
            android:name=".DisplayPromotionCodesActivity"
            android:exported="false" />

        <!-- Activity for displaying price items categorized by category -->
        <activity
            android:name=".DisplayPriceItemsActivity"
            android:exported="false" />

    </application>
</manifest>
